import { useState } from 'react';
import { SearchInputProps } from '../DataTable';

interface ToolBarProps {
  searchInputProps: SearchInputProps;
  allColumnFilterProps: object;
}

export default function ToolBar({
  searchInputProps,
  allColumnFilterProps,
}: ToolBarProps) {
  return <></>;
}
